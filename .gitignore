# Secrets — never commit real credentials
.env
.env.*
!.env.example

# Media — generated outputs, source clips, reference images. Keep these LOCAL, not in git.
# (Regenerate them via the pipeline / helper scripts; the repo ships code + docs only.)
*.mp4
*.mov
*.mkv
*.webm
*.avi
*.mp3
*.wav
*.png
*.jpg
*.jpeg
*.webp
*.gif

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# OS / editor cruft
.DS_Store
Thumbs.db
